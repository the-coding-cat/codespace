#include <stdio.h>

int main(void)
{
    // while (true) or while (1) same thing
    // while (flase) or while (0) same thing
    while (true)
    {
        printf("meow\n");
    }
    /*
    int i = 0;
    while (i < 3)
    {
        printf("meow\n");
        i++;
    }
    for (int f = 0; f < 3; f++)
    {
    printf("meow\n");
    }
    */
}